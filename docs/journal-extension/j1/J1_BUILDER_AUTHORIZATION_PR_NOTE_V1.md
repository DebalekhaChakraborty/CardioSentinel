# J1 — Builder Authorization PR Note V1

The authorization transition is intentionally staged for review before any controlled build is dispatched.

No build permission outside the exact V3-reviewed object is created. No scientific permission is created. The next executable action, after this transition is reviewed and merged, would be an explicit manual dispatch of the controlled environment build workflow under the single-claim policy.
